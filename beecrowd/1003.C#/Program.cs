using System;

namespace Program {
class URI {
    static void Main() {
        int A, B, Soma;
        A = int.Parse(Console.ReadLine());
        B = int.Parse(Console.ReadLine());
        Soma = A + B;
        Console.WriteLine("SOMA = " + Soma);
    }
}
}